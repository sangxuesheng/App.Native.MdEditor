---
description: 飞牛 fnOS 应用构建与部署规则，涵盖 fnpack 打包、appcenter-cli 安装、前端构建的标准流程与命令规范
globs: ["*.sh", "manifest", "cmd/*", "config/*", "wizard/*", "app/**"]
alwaysApply: true
---

# 飞牛 fnOS 应用构建与部署规则

## 项目基本信息

| 字段 | 值 |
|---|---|
| 应用 ID | `App.Native.MdEditor2` |
| 服务端口 | `18080` |
| 前端目录 | `app/ui/frontend/` |
| 构建产物 | `app/ui/frontend/dist/` |
| 打包产物 | `App.Native.MdEditor2.fpk` |
| NAS 访问地址 | `http://192.168.2.2:18080/` |

---

## 一、环境变量（飞牛 NAS 必须先执行）

```bash
export PATH=/var/apps/nodejs_v22/target/bin:$PATH
export PATH=/var/apps/python312/target/bin:$PATH
```

> 每次新开终端都需要重新执行，或写入 `~/.bashrc`。

---

## 二、标准构建部署命令

### 推荐方式：使用一体化脚本

| 场景 | 命令 |
|---|---|
| **默认构建 fpk / 完整发布**（优化打包 + 安装 + 重启） | `bash build-and-deploy.sh`（内部调用 `build-optimized.sh`） |
| **开发调试**（构建 + install-local + 重启，最快） | `bash build-and-deploy.sh --local` |
| 仅构建前端 | `bash build-and-deploy.sh --build` |
| 仅 fnpack 打包 | `bash build-and-deploy.sh --pack` |
| 仅安装已有 fpk + 重启 | `bash build-and-deploy.sh --install` |
| 仅重启应用 | `bash build-and-deploy.sh --restart` |

### 手动分步命令（备用）

```bash
# 1. 构建前端
cd /vol4/1000/开发文件夹/mac/app/ui/frontend
export PATH=/var/apps/nodejs_v22/target/bin:$PATH
npm run build

# 2. fnpack 打包
cd /vol4/1000/开发文件夹/mac
fnpack build

# 3a. 正式安装（fpk 文件）
appcenter-cli install-fpk App.Native.MdEditor2.fpk

# 3b. 开发快速安装（免打包，直接从源码目录安装）
cd /vol4/1000/开发文件夹/mac
appcenter-cli install-local

# 4. 重启与验证
appcenter-cli stop  App.Native.MdEditor2
sleep 2
appcenter-cli start App.Native.MdEditor2
curl -s http://localhost:18080/health
```

---

## 三、fnpack 命令规范

| 命令 | 用途 |
|---|---|
| `fnpack create <appname>` | 初始化标准应用项目结构 |
| `fnpack create <appname> --without-ui true` | 初始化纯服务类应用（无 UI） |
| `fnpack create <appname> --template docker` | 初始化 Docker 应用项目 |
| `fnpack build` | 在当前目录打包为 `.fpk` |
| `fnpack build --directory <path>` | 指定目录打包 |

### fnpack build 打包前必须通过的校验

| 路径 | 类型 | 规则 |
|---|---|---|
| `manifest` | 文件 | 必须存在，必选字段不能缺 |
| `config/privilege` | 文件 | 必须存在，合法 JSON |
| `config/resource` | 文件 | 必须存在，合法 JSON |
| `ICON.PNG` | 文件 | 必须存在（64×64） |
| `ICON_256.PNG` | 文件 | 必须存在（256×256） |
| `app/` | 目录 | 必须存在 |
| `cmd/` | 目录 | 必须存在 |
| `wizard/` | 目录 | 必须存在 |
| `app/{manifest.desktop_uidir}/` | 目录 | manifest 中有定义时必须存在 |

---

## 四、appcenter-cli 命令规范

### 安装类

| 命令 | 用途 |
|---|---|
| `appcenter-cli install-fpk <file.fpk>` | 安装 fpk 包（有向导时需交互） |
| `appcenter-cli install-fpk <file.fpk> --env config.env` | 静默安装（跳过交互向导） |
| `appcenter-cli install-local` | 从本地目录直接安装，免打包，**开发调试专用** |

### 应用管理类

| 命令 | 用途 |
|---|---|
| `appcenter-cli list` | 查看已安装应用列表 |
| `appcenter-cli start <appname>` | 启动指定应用 |
| `appcenter-cli stop <appname>` | 停止指定应用 |

### 系统配置类

| 命令 | 用途 |
|---|---|
| `appcenter-cli default-volume` | 查看默认安装存储空间 |
| `appcenter-cli default-volume 1` | 设置存储空间 1 为默认 |
| `appcenter-cli manual-install` | 查看手动安装功能状态 |
| `appcenter-cli manual-install enable` | 开启手动安装（用完及时关闭） |
| `appcenter-cli manual-install disable` | 关闭手动安装 |

---

## 五、静默安装配置文件格式

文件名：`config.env`，格式为键值对：

```ini
wizard_admin_username=admin
wizard_admin_password=mypassword123
wizard_database_type=sqlite
wizard_app_port=8080
wizard_agree_terms=true
```

> 变量名以 `wizard_` 开头，对应安装向导配置项。

---

## 六、开发工作流规范

```
修改代码
    ↓
bash build-and-deploy.sh --local     ← 开发调试（最快路径）
    ↓
验证功能：http://192.168.2.2:18080/
    ↓
准备发布时：
bash build-and-deploy.sh             ← 完整流程（构建+打包+安装+重启）
```

### AI 生成构建/部署相关代码时必须遵守的规则

1. **脚本开头必须设置 Node.js PATH**
   ```bash
   export PATH=/var/apps/nodejs_v22/target/bin:$PATH
   ```

2. **前端构建必须在 `app/ui/frontend/` 目录下执行 `npm run build`**，不得直接调用 `vite`。

3. **打包命令统一使用 `fnpack build`**，不得使用旧版本路径（如 `../fnpack-1.2.1-darwin-arm64`）。

4. **开发环境优先使用 `appcenter-cli install-local`**，正式发布才使用 `install-fpk`。

5. **每次安装后必须重启应用**：先 `stop` 再 `start`，间隔 `sleep 2`。

6. **修改 `manifest` 后必须重新执行完整流程**（`fnpack build` + `install-fpk`），`install-local` 不能替代。

7. **不得在脚本中硬编码 fnpack 的相对路径**，必须使用系统 PATH 中的 `fnpack` 命令。

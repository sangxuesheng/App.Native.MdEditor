# MD编辑器 - 全文件访问+指定格式适配+自动布局 完整功能需求
## 一、功能背景
现有MD编辑器仅显示.md格式文件，非支持格式直接拒绝打开；需优化为「显示所有文件+首批支持指定多格式+按文件类型自动切换默认布局」，保留用户手动切换双布局权限，提升灵活性与专业性。

## 二、核心前提（布局规则）
1. 编辑器支持3种布局模式：双栏模式（编辑区↔预览区）、仅编辑模式、仅预览模式；
2. 核心规则：打开**首批支持格式**文件时按类型自动切换默认布局，非首批支持格式按兜底规则处理；用户可在自动切换后手动切换为任意布局（含双栏），后续不强制切回；
3. 布局优先级：用户手动切换的布局优先级高于自动规则。

## 三、首批支持格式分类（核心定义）
| 格式分类       | 具体包含格式                                                                 | 核心处理规则                     |
|----------------|------------------------------------------------------------------------------|----------------------------------|
| MD格式         | .md                                                                          | 保留用户当前布局，预览区渲染富文本 |
| 文本类格式     | .text、.pdf（仅提取纯文本）、.env、.ini、.strm、.txt、.js、.ts、.html、.htm、.css、.scss、.less、.json、.py、.java、.c、.cpp、.cc、.cxx、.go、.rs、.php、.rb、.sh、.sql、.ml、.yaml、.yml、.vue | 自动切换为仅编辑模式，支持语法高亮 |
| 图片类格式     | .png、.jpg、.jpeg、.gif、.bmp、.webp（非首批，但单独适配预览）| 自动切换为仅预览模式，预览区展示图片 |
| 非支持格式     | 除上述所有格式外的其他格式（如.exe、.zip、.xlsx等）| 自动切换为仅编辑模式，提供纯文本/Hex兜底访问 |

## 四、核心需求
### 4.1 文件选择弹窗优化（基础层）
- 移除格式过滤：文件选择对话框默认展示当前目录下**所有文件**（含所有后缀名/无后缀名文件），不隐藏任何文件；
- 增强识别：文件列表显示「文件名+后缀名+文件大小+修改时间」，后缀名按分类着色：
  - MD格式（.md）：蓝色；
  - 文本类格式（如.py/.js/.vue等）：灰色；
  - 图片类格式：绿色；
  - 非支持格式（如.exe/.zip）：红色；
- 可选优化：保留“格式筛选”下拉框，默认选「所有文件」，并新增「首批支持格式」筛选选项（包含上述MD+文本类格式），方便用户快速筛选。

### 4.2 文件打开逻辑（核心层：自动布局+展示/编辑规则）
#### 第一步：按格式分类自动切换默认布局（打开文件时触发）
| 格式分类       | 自动切换的默认布局 | 布局切换提示（轻量，3秒消失）|
|----------------|--------------------|-----------------------------------------|
| MD格式（.md）| 保留用户当前布局   | 无（尊重用户习惯）|
| 文本类格式     | 仅编辑模式         | “文本类文件默认仅编辑模式打开，可手动切换双栏” |
| 图片类格式     | 仅预览模式         | “图片类文件默认仅预览模式打开，可手动切换双栏” |
| 非支持格式     | 仅编辑模式         | “非支持格式默认仅编辑模式打开，可手动切换双栏” |
| 异常文件       | 仅编辑模式         | “文件读取异常，默认仅编辑模式打开”|

#### 第二步：首批支持格式的展示/编辑规则
##### 场景1：MD格式（.md）
| 当前布局（用户/自动）| 编辑区行为                          | 预览区行为                          | 额外规则                     |
|------------------------|-------------------------------------|-------------------------------------|------------------------------|
| 任意布局               | 加载原文，支持MD语法高亮+编辑       | 渲染MD富文本（标题/列表/代码块等）| 手动切双栏后正常展示         |

##### 场景2：文本类格式（含所有指定格式）
| 当前布局               | 编辑区行为                                                                 | 预览区行为                          | 额外规则                     |
|------------------------|----------------------------------------------------------------------------|-------------------------------------|------------------------------|
| 自动切换：仅编辑       | 加载原文，支持对应语言语法高亮（如.py/python高亮、.vue/vue语法高亮）+ 编辑 | 预览区隐藏                          | 可手动切双栏/仅预览          |
| 手动切换：双栏         | 加载原文，支持对应语言语法高亮+编辑                                       | 纯文本展示（保留语法结构，不渲染）| 保持双栏布局                 |
| 手动切换：仅预览       | 后台加载内容（隐藏）| 纯文本展示（支持语法高亮）| 无编辑能力，仅查看           |
- 特殊说明：.pdf格式仅提取文件中的纯文本内容，不保留排版/图片，语法高亮按纯文本处理；.vue格式支持HTML/CSS/JS混合语法高亮。

##### 场景3：图片类格式（非首批，但适配预览）
| 当前布局               | 编辑区行为                          | 预览区行为                          | 额外规则                     |
|------------------------|-------------------------------------|-------------------------------------|------------------------------|
| 自动切换：仅预览       | 后台加载Hex/二进制内容（隐藏）| 等比缩放预览图片（支持点击放大）| 可手动切双栏/仅编辑          |
| 手动切换：双栏         | 加载Hex/二进制内容，支持编辑        | 等比缩放预览图片（实时刷新）| 保持双栏布局                 |
| 手动切换：仅编辑       | 加载Hex/二进制内容，支持编辑        | 预览区隐藏                          | 提示“切换双栏/仅预览可看图片” |

##### 场景4：非支持格式（兜底规则）
| 当前布局               | 编辑区行为                          | 预览区行为                          | 额外规则                     |
|------------------------|-------------------------------------|-------------------------------------|------------------------------|
| 自动切换：仅编辑       | 加载纯文本/Hex内容，支持编辑        | 预览区隐藏                          | 可手动切双栏/仅预览          |
| 手动切换：双栏         | 加载纯文本/Hex内容，支持编辑        | 显示「此格式不支持预览」| 保持双栏布局                 |
| 手动切换：仅预览       | 后台加载内容（隐藏）| 显示「此格式不支持预览」| 无编辑能力，仅查看提示       |
- 弹窗交互：打开非支持格式时，先自动切仅编辑模式，再弹出弹窗提供「纯文本/Hex/取消」访问选项。

### 4.3 编辑与保存规则（操作层）
#### 4.3.1 编辑规则
- 首批支持格式（MD+文本类）：在仅编辑/双栏布局下支持完整编辑，包含语法高亮、行号、缩进提示等专业能力；
- .pdf格式：仅能编辑提取后的纯文本内容，保存后仍为纯文本覆盖原文件（不还原PDF格式）；
- 所有格式：编辑区支持撤销/重做、查找替换、代码折叠（文本类格式）等基础编辑功能。

#### 4.3.2 保存规则
| 格式分类       | 保存触发逻辑                                                                 |
|----------------|------------------------------------------------------------------------------|
| MD格式         | 默认按原格式保存，无额外提示                                                 |
| 文本类格式     | ① 常规格式（.txt/.js/.py等）：默认按原格式保存，无额外提示；<br>② .pdf格式：保存时弹窗提示「仅保存提取的纯文本内容，原PDF格式将被覆盖，是否确认？」，选项：确认保存/另存为.md/取消 |
| 图片类格式     | 编辑Hex内容后保存，弹窗提示「修改图片二进制内容可能导致图片损坏，是否确认保存？」，选项：确认保存/另存为.md/取消 |
| 非支持格式     | 点击保存触发二次确认弹窗：<br>标题：保存非原生支持文件<br>内容：修改该格式文件可能导致文件损坏，是否确认保存？<br>选项：确认保存（原格式） / 另存为.md格式 / 取消 |
| Hex方式编辑文件  | 保存时将Hex字符串还原为二进制数据覆盖原文件，确认弹窗额外提示「Hex编辑保存会直接修改二进制数据」 |

### 4.4 手动切换布局的权限（核心兜底）
- 无论自动切换到何种默认布局，用户均可通过编辑器布局按钮手动切换为「双栏/仅编辑/仅预览」；
- 手动切换后，当前文件的布局状态锁定，直到用户再次切换或关闭文件；
- 新打开文件时，仍按“格式分类自动切换默认布局”执行，不继承上一文件的手动布局。

## 五、交互与体验要求
1. 语法高亮：首批支持的文本类格式需匹配对应语言的语法高亮规则（如Python的关键字、Vue的模板语法），无高亮错误；
2. 布局切换：自动切换响应时间≤300ms，切换无界面闪烁；
3. 弹窗规范：所有提示弹窗样式统一，按钮点击区域≥40px，支持ESC键关闭；
4. 性能要求：打开≤20MB的首批支持格式文件显示加载进度条，语法高亮渲染时间≤500ms；
5. 容错能力：编辑中文件被外部修改时，提示「文件已被修改，是否重新加载」，编辑器不崩溃；
6. PDF处理：提取PDF纯文本的准确率≥90%，复杂排版PDF仅提取可识别文本，不崩溃。

## 六、关键强制规则（必须遵守）
1. 格式覆盖：首批支持的所有格式需完整适配，无遗漏；.pdf仅处理纯文本提取，不做复杂解析；
2. 自动布局仅为默认：用户手动切换布局优先级最高，不强制回退；
3. 内容核心：所有文件内容仅加载到编辑区（后台），预览区仅做渲染/提示；
4. 编辑核心：仅编辑区具备编辑能力，预览区始终只读；
5. 双栏权限：任何格式都支持手动切换到双栏布局，无限制。

## 七、非需求排除项
1. 无需支持.pdf文件的图片/表格提取，仅提取纯文本；
2. 无需支持首批格式的自动格式化/代码检查（仅基础语法高亮）；
3. 无需自动修复损坏文件，仅按原始内容展示；
4. 无需支持网络文件打开，仅处理本地文件；
5. 无需跨会话同步布局偏好，仅当前会话有效。

## 八、示例场景（便于理解）
1. 用户打开.vue文件 → 自动切换为仅编辑模式（显示Vue语法高亮）→ 用户手动切双栏 → 编辑区显示Vue代码，预览区纯文本展示；
2. 用户打开.pdf文件 → 自动切换为仅编辑模式（显示提取的纯文本）→ 保存时弹窗提示「仅保存纯文本，是否确认」；
3. 用户打开.go文件 → 自动切换为仅编辑模式（Go语法高亮）→ 用户手动切仅预览 → 预览区显示带高亮的Go代码，编辑区隐藏；
4. 用户打开.md文件（当前为仅预览）→ 保留仅预览布局，预览区渲染MD富文本，可手动切双栏。
# 高亮文本测试

## 基础用法

这是普通文本，==这是高亮文本==，继续普通文本。

==整段高亮文本==

## 嵌套样式

==可以包含**粗体**文本==

==可以包含*斜体*文本==

==可以包含***粗斜体***文本==

==可以包含`代码`文本==

## 多个高亮

==第一个高亮== 普通文本 ==第二个高亮== 普通文本 ==第三个高亮==

## 实际应用场景

### 1. 标记重点

在学习笔记中，==重要概念==需要特别标记。

### 2. 强调关键信息

项目截止日期是 ==2026年3月15日==，请务必按时完成。

### 3. 代码注释

```javascript
// ==注意==：这个函数会修改原数组
function modifyArray(arr) {
  arr.push('new item');
}
```

### 4. 文档审阅

原文：这个功能已经完成。
修改：这个功能==尚未完成==。

## 与其他格式组合

- ==列表项中的高亮==
- 普通列表项
- ==另一个高亮列表项==

> ==引用块中的高亮文本==
> 
> 这是普通引用文本

| 表头1 | 表头2 |
|-------|-------|
| ==高亮单元格== | 普通单元格 |
| 普通单元格 | ==高亮单元格== |

## 注意事项

### ✅ 正确写法
==这是正确的高亮==

### ❌ 错误写法（有空格）
== 这是错误的高亮 ==

### ✅ 多行高亮
==这是
多行
高亮文本==

## 颜色对比

不同主题下的高亮效果：

- **浅色主题**：==黄色背景高亮== (#ffff00)
- **暗色主题**：==金色背景高亮== (#ffd700)
- **MD3 主题**：==浅黄色背景高亮== (#fff59d)

## 实用示例

### 学习笔记

**JavaScript 闭包**

闭包是指==函数可以访问其外部作用域的变量==，即使外部函数已经执行完毕。

关键点：
1. ==内部函数引用外部变量==
2. ==外部函数返回内部函数==
3. ==变量不会被垃圾回收==

### 会议纪要

**2026年3月1日 项目会议**

决议：
- ==下周一提交设计稿==
- 预算控制在 ==50万元== 以内
- ==张三负责前端开发==

### 代码审查

```python
def calculate_total(items):
    # ==TODO: 添加输入验证==
    total = sum(item.price for item in items)
    # ==FIXME: 处理空列表情况==
    return total
```

---

**语法**: `==文本==`  
**渲染**: <mark>文本</mark>  
**效果**: 黄色背景高亮

🎉 现在你可以使用 `==高亮文本==` 语法了！
